extends layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/users.css")

block content
  h1 Users
  if users.length > 0
    ul
      each user in users
        li #{user.name}
  else 
    h1 No Users Found! 